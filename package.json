{
  "name": "ftph-new",
  "version": "0.0.1",
  "scripts": {
    "clean": "mkdir -p builds && mkdir -p dist && rm -rf builds/* && rm -rf dist/*",
    "setup": "npm run setup-cordova && npm run convert-audio",
    "setup-cordova": "scripts/setup-cordova",
    "build-cordova": "scripts/compile && scripts/build-cordova",
    "convert-audio": "node scripts/convert-audio.js",
    "compile": "scripts/compile",
    "dev-server": "scripts/dev-server",
    "xcode": "open ./builds/cordova/platforms/ios/ftph.xcodeproj"
  },
  "dependencies": {
    "howler": "^1.1.26",
    "lodash": "^3.10.1",
    "rx": "^3.1.2"
  },
  "devDependencies": {
    "audio-converter": "^1.0.7",
    "babel-core": "^5.8.24",
    "babel-loader": "^5.3.2",
    "file-loader": "^0.8.4",
    "glob": "^5.0.14",
    "loader-utils": "^0.2.11",
    "pace": "0.0.4",
    "progress": "^1.1.8",
    "webpack": "^1.12.1"
  }
}
